include mixins.jade

+breadcrumbs(page.title, 'Subtitle', [['home', '/',], [ 'current', page.get_absolute_url]])

section.text_block
    .container
        .row!= page.created
        .row!= page.mark_current_regexes
        .row!= page.meta_description
        .row!= page.move_url
